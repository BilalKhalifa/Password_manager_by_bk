# Copy this file to .env and fill values. Do NOT commit .env (it's ignored).
# TiDB / MySQL settings
DB_HOST=gateway01.ap-southeast-1.prod.aws.tidbcloud.com
DB_PORT=4000
DB_USER=2Xr1GuCBUH9StSP.root
DB_PASS=8jkwogEUuOsAbAN4
DB_NAME=password_manager

# Path to CA certificate file (relative to repo root). Optional if using default certs/ca-cert.pem
CA_CERT_PATH=certs/ca-cert.pem

# JWT secret for signing authentication tokens (set a long random value in production)
JWT_SECRET=change-me-to-a-secure-secret

# Optional seed credentials
SEED_USER=testuser
SEED_PASS=testpass
